// WHO a transition belongs to — an IDENTITY, not a flag.
//
// The problem: `own: true/false` tried to be that identity with two values,
// one of them unnamed ([[id:lex-unmarked]]). A boolean cannot be a map key,
// so two witnesses could not hold a record for one document at all — the
// child drafting over a friend's page is exactly that case.
//
// Kernel, not weave or turtle: the seating law indexes records by it, and
// the canvas scopes its hatch gate by it. Two spellings of one identity is a
// place they can disagree, silently — a gate written for a witness nobody is.
//
// Two values today. PEER is every watched friend at once; the ADDR names
// which friend. When a third witness is minted, nothing here enumerates
// the set of values — weave/page.js asks who stands on a document instead.

export const SELF = "self"
export const PEER = "peer"
